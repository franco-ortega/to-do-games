import { TimeSpanPaths } from "./types";

export const timeSpanOptions = {
  week: "week" as TimeSpanPaths,
  month: "month" as TimeSpanPaths,
  year: "year" as TimeSpanPaths
}

export enum TimeSpanOptions {
  week = 'week',
  month = 'month',
  year = 'year'
}