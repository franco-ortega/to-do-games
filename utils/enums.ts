import { TimeSpan } from "./types"

export const timeSpanOptions = {
  week: {
    path: "week",
  } as TimeSpan,
  month: {
    path: "month",
  } as TimeSpan,
  year: {
    path: "year",
  } as TimeSpan
}