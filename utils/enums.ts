import { TimeSpanPaths, TimeSpanHeaders, TimeSpanOptions } from "./types"

export const timeSpanOptions = {
  week: {
    path: "week",
    header: "Week"
  } as TimeSpanOptions,
  month: {
    path: "month",
    header: "Month"
  } as TimeSpanOptions,
  year: {
    path: "year",
    header: "Year"
  } as TimeSpanOptions
}