import { TimeSpan } from "./types"

export const timeSpanOptions = {
  week: {
    path: "week",
    header: "Week"
  } as TimeSpan,
  month: {
    path: "month",
    header: "Month"
  } as TimeSpan,
  year: {
    path: "year",
    header: "Year"
  } as TimeSpan
}